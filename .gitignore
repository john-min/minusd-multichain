# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
id_rsa*
keystore/
**/keystore/
**/UTC--*

# Foundry build + broadcast artifacts
out/
cache/
broadcast/
evm/out/
evm/cache/
evm/broadcast/

# Local validator / Solana artifacts
test-ledger/
.anchor/
target/
**/*.so
**/id.json
**/.sbf/
solana/.anchor/
solana/test-ledger/
solana/target/

# Node (future CLI / Lab)
node_modules/

# OS / editor
.DS_Store
.idea/
.vscode/
